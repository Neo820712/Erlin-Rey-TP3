# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Entornos virtuales (uv crea .venv)
.venv/
venv/

# Variables de entorno y secretos
.env

# Base de datos SQLite generada (la carpeta se versiona con data/.gitkeep)
data/*.db
data/*.sqlite
data/*.sqlite3

# Precios cacheados por yfinance (la carpeta se versiona con data/prices_cache/.gitkeep)
data/prices_cache/*.csv

# Config local de Claude Code (personal, no se comparte)
.claude/settings.local.json

# Sistema operativo
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
